package controller;

import model.ReversiModel;
import tiles.Tile;

public class ReversiGUIController implements ReversiController {
  @Override
  public void playGame(ReversiModel model, int numRows, int numColumns, Tile type) {

  }
}
